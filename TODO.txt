Parsing

-- Add System ligands 
-- Add neuclotide models
--fix the ring systems
--fix flexible rings
--add Charmm force family


PACKAGE_MANAGEMENT

-- in precomputed data.py setup tools and pkg_resources is used to laod data,
    this is depreciated in setuptools versions >82, change this to use pytohn built it
OutPUT 

-- add a verbose output option so it only writes the requested analysis output by default

--alphamask 

work out which stage is slow and refactor


--resample subsite 

Binding site
--binding-site add a manual site pathway.


TOOLS

