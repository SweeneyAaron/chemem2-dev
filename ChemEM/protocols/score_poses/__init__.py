# Standalone, deletable offline pose re-scorer for orchestrator scoring-function
# development. NOT registered in ChemEM.protocol_spec.REGISTRY — run directly:
#     python -m ChemEM.protocols.score_poses.score_poses CONFIG [options]
# Delete this directory to remove the tool entirely; nothing else depends on it.
